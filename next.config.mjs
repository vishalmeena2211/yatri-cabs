/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {
                hostname:"www.yatricabs.com"
            }
        ]
    }
};

export default nextConfig;
